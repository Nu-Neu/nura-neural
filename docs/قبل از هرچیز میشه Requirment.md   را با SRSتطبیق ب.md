<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# قبل از هرچیز میشه Requirment.md   را با SRSتطبیق بدیم فکر میکنم به نظر من هم داکیومنت های جدید رو فارسی بنویسیم

عالی! الان هر دو فایل را دارم. بذارید اول تحلیل کنم چه گپ‌هایی وجود داره، بعد یک نسخه به‌روز شده تولید می‌کنم.[^1][^2]

***

## 🔍 تحلیل اختلافات بین `02-requirements.md` و `SRS v2.4`

| بخش | `02-requirements.md` (v1.0 Draft) | `SRS v2.4` (Approved) | وضعیت |
| :-- | :-- | :-- | :-- |
| **نسخه** | 1.0 (draft) | 2.4 (approved) | ⚠️ عقب‌مانده |
| **User Stories** | 5 تا (سطح بالا) | 20+ تا (با Acceptance Criteria دقیق) | ❌ ناقص |
| **Functional Requirements** | 6 System-Level (`REQ-SYS-XXX`) | 15+ Module-Based (`REQ-ING`, `REQ-AI`, `REQ-API`, `REQ-UI`, `REQ-DB`) | ❌ ساختار قدیمی |
| **NFRs** | 8 تا (کلی) | جداول دقیق Performance, Scalability, Security, Reliability, Maintainability, Usability | ⚠️ کم‌عمق |
| **Testing** | ❌ وجود ندارد | Section 6: End-to-End Scenarios (5 سناریو با Pass Criteria) | ❌ گم‌شده |
| **Traceability** | ❌ وجود ندارد | Appendix A: Traceability Matrix (REQ → ENG-SPEC mapping) | ❌ گم‌شده |
| **Risk Register** | ❌ وجود ندارد | Section 5.2: Risk Register (5 ریسک با Mitigation) | ❌ گم‌شده |
| **Glossary** | ✅ 7 اصطلاح | ✅ 9 اصطلاح | ✅ خوب |


***

## ⚠️ نکته مهم درباره زبان مستندات

شما پیشنهاد دادید مستندات جدید را **فارسی** بنویسیم. این مزایا و معایب دارد:[^2][^1]


| مزایا ✅ | معایب ❌ |
| :-- | :-- |
| تیم ایرانی راحت‌تر می‌خواند (Amir، Navid، Reyhaneh، Sina، Saman) | GitHub standard: مستندات فنی معمولاً انگلیسی است برای همکاری باز |
| مفاهیم دامنه‌ای (مثل "رسانه رژیم"، "کارشناس اطلاعات") طبیعی‌تر ترجمه می‌شوند | ابزارهای خودکار (CI/CD، Linters، OpenAPI generators) فارسی را پشتیبانی نمی‌کنند |
| مستندسازی سریع‌تر (فکر کردن به فارسی، نوشتن به فارسی) | اگر بعداً بخواهید contributor خارجی اضافه کنید، مشکل خواهد بود |

### 💡 پیشنهاد من (رویکرد ترکیبی):

1. **نسخه فارسی برای مرور داخلی تیم** (این همان چیزی است که الان تولید می‌کنم)
2. **نسخه انگلیسی برای GitHub** (که بعداً از روی فارسی ترجمه می‌شود یا به‌صورت موازی نگهداری می‌شود)
3. **از Frontmatter استفاده کنیم تا زبان را علامت بزنیم:**

```yaml
---
doc_type: requirements
version: 2.5
language: fa
english_version: docs/en/02-requirements.md
---
```


***

## 📄 نسخه جدید `02-requirements.md` (فارسی - پیش‌نویس)

حالا یک نسخه به‌روز شده که **با SRS v2.4 همخوان است** تولید می‌کنم. این نسخه:

- همه Requirements را از SRS می‌گیرد
- ساختار ماژولار (`REQ-ING`, `REQ-AI`, etc) را حفظ می‌کند
- بخش Testing و Traceability را اضافه می‌کند
- به فارسی است

***

```markdown
---
doc_type: requirements
version: 2.5
last_updated: 2026-02-04
owner: تیم نورا نِرُال
status: draft
language: fa
english_version: docs/en/02-requirements.md
traceability: [Ref: 01-discovery-and-planning.md, Ref: SRS v2.4]
---

# مستندات الزامات - پلتفرم نورا (نسخه 2.5)

## کنترل نسخه

| فیلد | مقدار |
|:---|:---|
| **نسخه** | 2.5 (پیش‌نویس) |
| **تاریخ** | ۴ فوریه ۲۰۲۶ |
| **مالک** | تیم نورا نِرُال |
| **وضعیت** | پیش‌نویس (Draft) |
| **منبع** | SRS v2.4 (Approved) |

### تاریخچه تغییرات

| نسخه | تاریخ | مشارکت‌کنندگان | تغییرات |
|:---|:---|:---|:---|
| 1.0 | ۴ فوریه ۲۰۲۶ | تیم محصول | نسخه اولیه با User Stories کلی |
| 2.5 | ۴ فوریه ۲۰۲۶ | Facilitator + تیم | همگام‌سازی با SRS v2.4: افزودن REQ-ING/AI/API/UI/DB، NFRها، سناریوهای تست |

---

## ۱. الزامات کارکردی (Functional Requirements)

### ۱.۱ ماژول: لایه دریافت داده (Data Ingestion Layer)

#### REQ-ING-001: دریافت خودکار از فیدهای RSS

**داستان کاربر:**
به‌عنوان **مدیر سیستم**، می‌خواهم پلتفرم به‌طور خودکار اخبار را از فیدهای RSS پیکربندی‌شده هر ۱۵ دقیقه یک‌بار دریافت کند تا پایگاه‌داده همیشه آخرین مقالات را داشته باشد.

**معیارهای پذیرش:**
1. Workflow در n8n هر ۱۵ دقیقه اجرا شود (cron: `*/15`)
2. Workflow لیست ۵۰۰ منبع RSS از جدول `source_profiles` پردازش کند
3. هر آیتم دریافت‌شده با استفاده از `url_hash` (SHA-256) برای تکراری بودن بررسی شود
4. اگر تکراری بود، آیتم Skip شود و با دلیل `DUPLICATE_URL` لاگ شود
5. عکس HTML خام در Azure Blob Storage (Hot Tier) ذخیره شود با مسیر: `raw/{source_id}/{date}/{url_hash}.html`
6. متادیتا (عنوان، متن، نویسنده، تاریخ انتشار) با GPT-4o-mini استخراج شود
7. داده استخراج‌شده در جدول `items` با وضعیت `PENDING_ANALYSIS` درج شود

**الزامات عملکردی:**
- زمان پردازش هر آیتم: ≤۵ ثانیه (P95)
- اندازه دسته: ۵۰ آیتم در هر اجرا
- نرخ خطا: <۲٪ (شکست‌های شبکه استثنا)

[Ref: REQ-ING-001]

---

#### REQ-ING-002: دریافت داده از Twitter/X

**داستان کاربر:**
به‌عنوان **مدیر سیستم**، می‌خواهم پلتفرم اکانت‌ها و هشتگ‌های خاص توییتر را رصد کند تا ژورنالیسم شهروندی و به‌روزرسانی‌های لحظه‌ای ثبت شوند.

**معیارهای پذیرش:**
1. یکپارچه‌سازی Twitter API v2 از طریق n8n (احراز هویت OAuth 2.0)
2. رصد ۲۰۰ اکانت از Source Bible (روزنامه‌نگاران شهروندی تأییدشده، فعالان)
3. رصد هشتگ‌ها: `#IranProtests`, `#MahsaAmini`, `#OpIran` (قابل تنظیم)
4. فاصله زمانی polling: هر ۵ دقیقه برای اکانت‌های اولویت‌دار، ۱۵ دقیقه برای بقیه
5. توییت‌ها با استفاده از `tweet_id` (کلید اصلی) dedup شوند
6. پیوست‌های رسانه‌ای (تصاویر/ویدیوها) به Azure Blob Storage دانلود شوند
7. متادیتای توییت استخراج شود: `author_handle`, `verified_status`, `retweet_count`, `like_count`, `timestamp`

**موارد خاص (Edge Cases):**
- **توییت‌های حذف‌شده:** علامت بزن به `DELETED`، از DB پاک نکن
- **اکانت‌های معلق‌شده:** هشدار ثبت کن، رصد را برای ۲۴ ساعت غیرفعال کن

[Ref: REQ-ING-002]

---

#### REQ-ING-003: حذف تکراری‌ها (Deduplication)

**داستان کاربر:**
به‌عنوان **مهندس داده**، می‌خواهم سیستم مقالات تکراری را تشخیص داده و فیلتر کند تا هزینه‌های ذخیره‌سازی به حداقل برسد و کیفیت تحلیل حفظ شود.

**معیارهای پذیرش:**
1. **Dedup بر اساس URL:** محاسبه SHA-256 hash از URL استاندارد (حذف UTM params، trailing slash)
2. **Dedup بر اساس محتوا:** محاسبه SimHash از `title + body_text` (امضای ۱۰۲۴ بیتی)
3. اگر شباهت SimHash ≥۹۵٪ با آیتم موجود در ۲۴ ساعت گذشته، علامت بزن `REPRINT`
4. Reprintها به آیتم اصلی لینک شوند از طریق `original_item_id` (FK به `items.id`)
5. Reprintها Trust Scoring را trigger نکنند (امتیاز را از اصلی به ارث ببرند)
6. منطق Dedup **قبل از** استخراج GPT-4o-mini اجرا شود تا هزینه API صرفه‌جویی شود

**عملکرد:**
- بررسی dedup: ≤۵۰ms برای هر آیتم (با استفاده از `url_hash` و `content_hash` ایندکس‌شده)

[Ref: REQ-ING-003]

---

#### REQ-ING-004: فیلتر زبانی

**داستان کاربر:**
به‌عنوان **کنترل‌کننده هزینه**، می‌خواهم سیستم مقالات غیر از زبان‌های پشتیبانی‌شده را دور بریزد تا هزینه‌های API برای محتوای غیرمرتبط هدر نرود.

**معیارهای پذیرش:**
1. تشخیص زبان با استفاده از کتابخانه `langdetect` (Python) یا GPT-4o-mini (اگر استخراج metadata قبلاً trigger شده باشد)
2. زبان‌های پشتیبانی‌شده: انگلیسی (EN)، فارسی (FA)، عربی (AR)
3. آیتم‌هایی که در زبان‌های پشتیبانی‌شده نیستند با وضعیت `LANGUAGE_MISMATCH` علامت بزن
4. آیتم‌های دورریخته‌شده با کد زبان تشخیص‌داده‌شده (ISO 639-1) لاگ شوند
5. آیتم‌های دورریخته‌شده در جدول `items` ذخیره **نشوند** (فیلتر قبل از درج)

**استثنا:**
- اگر `source_class = INTL_WIRE` (Reuters، AP، AFP)، همه زبان‌ها را برای گسترش آینده مجاز کن

[Ref: REQ-ING-004]

---

### ۱.۲ ماژول: لایه استدلال هوش مصنوعی (AI Reasoning Layer)

#### REQ-AI-001: محاسبه Trust Score

**داستان کاربر:**
به‌عنوان **تحلیلگر**، می‌خواهم هر آیتم خبری یک Trust Score (۱۵-۹۵) داشته باشد تا بدون خواندن مقاله کامل بتوانم سریعاً اعتبار را ارزیابی کنم.

**معیارهای پذیرش:**
1. Trust Score با استفاده از **فرمول قطعی** محاسبه شود (ببینید [ENG-SPEC v3.1، بخش ۴])
2. اجزای فرمول:
   - **مشارکت پایه (۰-۴۵ امتیاز):** بر اساس `source_profiles.base_score`
   - **منشأ (۰-۲۰ امتیاز):** اعتبار URL، timestamp، نام نویسنده، dateline، رسانه
   - **تأیید متقابل (۰-۲۰ امتیاز):** منابع مستقل که ادعای مشابه را تأیید می‌کنند
   - **شفافیت (۰-۱۵ امتیاز):** افشای سردبیری، سیاست اصلاحات
   - **ضرایب تعدیل (-۱۵ تا +۱۰ امتیاز):** پرچم‌های قرمز (منبع ناشناس)، پرچم‌های سبز (اسناد اولیه)
3. امتیاز نهایی بین ۱۵ و ۹۵ محدود شود (بدون استثنا)
4. تأخیر محاسبه: ≤۶۰ ثانیه برای هر آیتم (شامل جستجوی برداری برای تأیید متقابل)
5. نتیجه در جدول `trust_signals` ذخیره شود با فیلدهای:
   - `final_score` (INT)
   - `trust_level` (ENUM: HIGH، MEDIUM، LOW)
   - `breakdown_json` (JSONB با امتیازهای اجزا)
   - `explanation` (TEXT، خلاصه قابل‌فهم برای انسان)

**محدوده امتیاز → سطح اعتماد → رنگ نشان UI:**

| محدوده امتیاز | سطح اعتماد | رنگ نشان |
|:---|:---|:---|
| ۷۰-۹۵ | بالا (HIGH) | سبز |
| ۴۰-۶۹ | متوسط (MEDIUM) | زرد/عنبری |
| ۱۵-۳۹ | پایین (LOW) | قرمز |

[Ref: REQ-AI-001] [See: ENG-301 to ENG-306 for detailed formula]

---

#### REQ-AI-002: خوشه‌بندی روایت‌ها (Narrative Clustering)

**داستان کاربر:**
به‌عنوان **کاربر**، می‌خواهم مقالات مرتبط درباره یک رویداد مشابه گروه‌بندی شوند تا تیترهای تکراری نبینم و بتوانم توسعه داستان را دنبال کنم.

**معیارهای پذیرش:**
1. خوشه‌بندی از **pgvector** برای شباهت معنایی استفاده می‌کند (فاصله کسینوسی روی embeddings با ۱۵۳۶ بُعد)
2. پنجره خوشه‌بندی: ۱۴ روز (قابل تنظیم بر اساس نوع موضوع—ببینید [ENG-402])
3. شرط تطابق:
   - **شباهت کسینوسی ≥۰.۸۵** یا
   - **شباهت کسینوسی ≥۰.۷۵ و همپوشانی موجودیت ≥۲** (موجودیت‌های مشترک PERSON/ORG/EVENT)
4. آیتم‌هایی با `MainEventID` یکسان همیشه ادغام شوند (حتی اگر موضع‌گیری‌های مخالف داشته باشند)
5. روایت‌های جدید GPT-4o-mini (o3-mini ترجیحاً) را trigger می‌کنند تا عنوان خلاصه تولید کنند
6. فرمت عنوان روایت: `[MainEvent]: [KeyDevelopment]` (مثلاً «اعتراضات زاهدان: شمار کشته‌شدگان به ۹۶ رسید»)
7. خوشه‌بندی دسته‌ای هر ۱۵ دقیقه اجرا می‌شود (هماهنگ با workflow دریافت داده)

**عملکرد:**
- جستجوی برداری: ≤۲۰۰ms برای هر query (ایندکس HNSW روی `items.embedding`)
- تأخیر خوشه‌بندی: ≤۵ ثانیه برای هر آیتم جدید

[Ref: REQ-AI-002] [See: ENG-401, ENG-402]

---

#### REQ-AI-003: تشخیص منابع پروکسی (Proxy Detection)

**داستان کاربر:**
به‌عنوان **افسر اعتماد و ایمنی**، می‌خواهم سیستم رسانه‌های وابسته به دولت که خود را مستقل جا می‌زنند شناسایی کند تا نتوانند از طریق تأیید جعلی trust score را به‌طور مصنوعی افزایش دهند.

**معیارهای پذیرش:**
1. کار دسته‌ای هفتگی **ProxyScore (۰-۱۰۰)** را برای همه منابع با فرمول زیر محاسبه کند:
```

ProxyScore = 0.3×ContentOverlap + 0.3×NarrativeAlign + 0.2×AmplificationNet + 0.2×TechOverlap

```
2. محاسبات اجزا:
- **ContentOverlap:** درصد مقالات با شباهت کسینوسی ≥۰.۹۰ به منابع شناخته‌شده رژیم
- **NarrativeAlign:** درصد روایت‌هایی که منبع هیچ‌وقت با قاب‌بندی رژیم مخالفت نمی‌کند
- **AmplificationNet:** تقویت رسانه اجتماعی از شبکه‌های باتی شناخته‌شده رژیم (۷۵۰۰ اکانت)
- **TechOverlap:** زیرساخت میزبانی/IP مشترک با سایت‌های رژیم
3. اقدامات بر اساس آستانه:

| ProxyScore | برچسب | اقدام |
|:---|:---|:---|
| ≥۷۰ | **پروکسی دولتی** | طبقه‌بندی مجدد `source_class = REGIME_MEDIA` یا اعمال جریمه -۱۰ به base score |
| ۴۰-۶۹ | **منطقه خاکستری** | پرچم هشدار «وابسته به دولت»، نیاز به ممیزی انسانی قبل از تأیید متقابل |
| <۴۰ | **مستقل** | بدون جریمه |

4. **محدودیت حیاتی:** منابعی که به‌عنوان «پروکسی دولتی» پرچم خورده‌اند **نمی‌توانند** منابع رژیم دیگر را تأیید کنند
5. همه طبقه‌بندی‌های مجدد در جدول `audit_log` با دلیل و تأییدکننده ثبت شوند

[Ref: REQ-AI-003] [See: ENG-202]

---

#### REQ-AI-004: مدیریت بیانیه‌های رسمی (Statement of Record)

**داستان کاربر:**
به‌عنوان **Fact-Checker**، می‌خواهم سیستم بین بیانیه نظر یک VIP و ادعاهای واقعی تمایز بگذارد تا شخصیت‌های سیاسی نتوانند ادعاهای تأییدنشده را به‌طور مصنوعی تقویت کنند.

**معیارهای پذیرش:**
1. زمانی که `source_class = KEY_FIGURE` (مثلاً Trump، Pezeshkian، Pahlavi):
- **تشخیص «بیانیه رسمی»** (مثلاً «من خواهم...»، «ما برنامه داریم...»، «من محکوم می‌کنم...»)
- پرچم بزن `is_official_statement = TRUE`
- Trust Score = امتیاز اصالت (بالا اگر گوینده از طریق کانال رسمی تأیید شود)
2. **تشخیص «ادعای واقعی»** (مثلاً «۵۰۰ نفر کشته شدند»، «آن‌ها به ما حمله کردند»)
- علامت بزن `UNVERIFIED_CLAIM`
- حداقل ۱ تأیید متقابل مستقل نیاز دارد (NGO، Wire و غیره)
- اگر تأیید متقابل یافت نشد، امتیاز **پایین** باقی بماند (≤۴۰)
3. وضعیت VIP truth score ادعاهای واقعی را تقویت **نمی‌کند**—فقط تأیید مستقل امتیاز را افزایش می‌دهد

**مثال:**
- ترامپ توییت می‌کند: *«من ایران را متقاعد کردم ۸۰۰ اعدام را لغو کند»*
- **بیانیه:** «من متقاعد کردم...» → `is_official_statement = TRUE`، اصالت بالا
- **ادعا:** «۸۰۰ اعدام لغو شد» → نیاز به تأیید از Iran Human Rights، Amnesty
- اگر تأیید نشد → امتیاز = ۴۰ (پایه) + ۱۰ (منشأ) + ۰ (تأیید متقابل) = ۵۰ (متوسط)، پرچم «تأییدنشده»

[Ref: REQ-AI-004] [See: ENG-201]

---

### ۱.۳ ماژول: لایه API محصول (Product API Layer)

#### REQ-API-001: نقطه پایانی خوراک عمومی (Public Feed Endpoint)

**داستان کاربر:**
به‌عنوان **توسعه‌دهنده فرانت‌اند**، می‌خواهم یک نقطه پایانی feed که روایت‌ها را با آیتم‌های تودرتو برمی‌گرداند تا بتوانم آخرین اخبار را در صفحه اصلی نمایش دهم.

**معیارهای پذیرش:**
1. **Endpoint:** `GET /api/v1/feed`
2. **پارامترهای Query:**
- `limit` (INT، پیش‌فرض ۲۰، حداکثر ۱۰۰)
- `offset` (INT، پیش‌فرض ۰، برای صفحه‌بندی)
- `language` (ENUM: EN، FA، AR، پیش‌فرض EN)
- `min_trust_score` (INT، پیش‌فرض ۴۰، محدوده ۱۵-۹۵)
3. **طرح پاسخ (Response Schema):**
```json
{
"narratives": [
 {
   "narrative_id": "uuid",
   "title": "اعتراضات زاهدان: شمار کشته‌شدگان به ۹۶ رسید",
   "summary": "تولیدشده توسط GPT-4o-mini...",
   "created_at": "2026-02-03T10:00:00Z",
   "last_updated": "2026-02-03T11:00:00Z",
   "item_count": 12,
   "avg_trust_score": 72,
   "top_items": [
     {
       "item_id": "uuid",
       "title": "HRANA گزارش می‌دهد ۹۶ کشته در زاهدان",
       "source_name": "خبرگزاری فعالان حقوق بشر",
       "source_logo_url": "https://...",
       "publish_date": "2026-02-03T09:30:00Z",
       "trust_score": 85,
       "trust_level": "HIGH",
       "url": "https://..."
     }
   ]
 }
],
"pagination": {
 "total": 150,
 "limit": 20,
 "offset": 0,
 "has_next": true
}
}
```

4. **عملکرد:** زمان پاسخ ≤۵۰۰ms (P95)
5. **کَشینگ:** کَش Redis با TTL دو دقیقه

[Ref: REQ-API-001]

---

#### REQ-API-002: نقطه پایانی جزئیات آیتم

**داستان کاربر:**
به‌عنوان **کاربر**، می‌خواهم روی یک آیتم خبری کلیک کنم و مقاله کامل را با تفکیک trust score ببینم تا بفهمم چرا امتیاز بالا یا پایین است.

**معیارهای پذیرش:**

1. **Endpoint:** `GET /api/v1/items/{item_id}`
2. **طرح پاسخ:**
```json
{
  "item_id": "uuid",
  "title": "...",
  "body_text": "محتوای کامل مقاله...",
  "source": {
    "name": "HRANA",
    "logo_url": "https://...",
    "source_class": "NGO_WATCHDOG",
    "base_score": 90
  },
  "metadata": {
    "author": "جین دو",
    "publish_date": "2026-02-03T09:30:00Z",
    "url": "https://...",
    "entities": [
      {"type": "PERSON", "name": "مهسا امینی"},
      {"type": "EVENT", "name": "اعتراضات زاهدان"}
    ]
  },
  "trust_signal": {
    "final_score": 85,
    "trust_level": "HIGH",
    "badges": ["VERIFIED_SOURCE", "CORROBORATED"],
    "breakdown": {
      "base": 40,
      "provenance": 20,
      "corroboration": 18,
      "transparency": 12,
      "modifiers": -5
    },
    "explanation": "اعتبار بالا به دلیل منبع NGO، تأییدات چندگانه، و منبع‌یابی شفاف.",
    "warnings": ["جزئی: ساکنان ناشناس نقل‌شده‌اند"]
  },
  "narrative": {
    "narrative_id": "uuid",
    "title": "اعتراضات زاهدان: شمار کشته‌شدگان به ۹۶ رسید"
  }
}
```

3. **عملکرد:** زمان پاسخ ≤۳۰۰ms (P95)

[Ref: REQ-API-002]

---

### ۱.۴ ماژول: لایه پایگاه‌داده و ذخیره‌سازی

#### REQ-DB-001: طرح PostgreSQL

**داستان کاربر:**
به‌عنوان **مدیر پایگاه‌داده**، می‌خواهم یک طرح نرمال‌شده با ایندکس‌گذاری برداری داشته باشم تا queryها سریع باشند و یکپارچگی داده حفظ شود.

**معیارهای پذیرش:**

1. PostgreSQL 16 با افزونه **pgvector** فعال
2. جداول اصلی:
    - `source_profiles` (۵۰۰ منبع با `base_score`، `source_class`، `ownership_cluster`)
    - `items` (مقالات خبری با `embedding` vector[^1536])
    - `trust_signals` (امتیازهای اعتماد با `breakdown_json`)
    - `narratives` (داستان‌های خوشه‌بندی‌شده)
    - `audit_log` (ردیابی تغییرات)
3. ایندکس‌ها:
    - **ایندکس HNSW** روی `items.embedding` (HNSW با m=16، ef_construction=64)
    - ایندکس‌های B-tree روی `items.publish_date`، `items.source_id`، `items.narrative_id`
    - ایندکس GIN روی `items.metadata` (JSONB)
4. محدودیت‌ها:
    - `trust_score` CHECK (۱۵ ≤ `final_score` ≤ ۹۵)
    - `source_class` ENUM (طبقه‌بندی دقیق، بدون رشته‌های آزاد)

[Ref: REQ-DB-001]

---

#### REQ-DB-002: Azure Blob Storage

**داستان کاربر:**
به‌عنوان **مدیر هزینه**، می‌خواهم عکس‌های HTML خام در blob storage ارزان ذخیره شوند تا اندازه پایگاه‌داده قابل مدیریت بماند.

**معیارهای پذیرش:**

1. حساب ذخیره‌سازی: `nura_storage_acct`
    - **Tier داغ:** ۰-۳۰ روز
    - **Tier سرد:** ۳۱-۹۰ روز
    - **Tier آرشیو:** >۹۰ روز
2. ساختار Container:
    - `raw/{source_id}/{YYYY-MM-DD}/{url_hash}.html`
    - `media/{item_id}/{filename}.jpg|png|mp4`
3. سیاست نگهداری: حذف خودکار پس از ۱ سال (قابل تنظیم)
4. دسترسی: توکن‌های SAS با انقضای ۱ ساعته برای بازیابی فرانت‌اند

[Ref: REQ-DB-002]

---

## ۲. الزامات غیرکارکردی (Non-Functional Requirements)

### ۲.۱ عملکرد (Performance)

| معیار | هدف | اندازه‌گیری |
| :-- | :-- | :-- |
| زمان پاسخ API (P95) | ≤۵۰۰ms | نقطه پایانی `/feed` |
| محاسبه Trust Score | ≤۶۰s برای هر آیتم | از دریافت تا درج در `trust_signals` |
| تأخیر جستجوی برداری | ≤۲۰۰ms | query ANN در pgvector (۵۰ نتیجه برتر) |
| زمان Batch خوشه‌بندی | ≤۵ دقیقه | پردازش ۵۰۰ آیتم جدید |
| زمان Query پایگاه‌داده (P95) | ≤۱۰۰ms | queryهای ساده SELECT |

[Ref: REQ-NFR-001]

---

### ۲.۲ مقیاس‌پذیری (Scalability)

| بُعد | هدف MVP | هدف فاز ۲ |
| :-- | :-- | :-- |
| آیتم‌های دریافت‌شده/روز | ۵۰۰۰ | ۵۰۰۰۰ |
| درخواست API/دقیقه | ۱۰۰ | ۱۰۰۰ |
| کاربران همزمان | ۵۰۰ | ۱۰۰۰۰ |
| اندازه پایگاه‌داده | ۱۰۰ GB | ۱ TB |
| اندازه ایندکس برداری | ۱۰M آیتم | ۱۰۰M آیتم |

[Ref: REQ-NFR-002]

---

### ۲.۳ امنیت (Security)

| الزام | پیاده‌سازی |
| :-- | :-- |
| احراز هویت API | نقاط پایانی عمومی (بدون احراز هویت برای MVP)؛ فاز ۲: کلیدهای API + JWT |
| رمزگذاری پایگاه‌داده | Transparent Data Encryption (TDE) فعال |
| رمزگذاری Blob Storage | AES-256 در حالت سکون، HTTPS در حین انتقال |
| اعتبارسنجی ورودی | ضدعفونی همه ورودی‌های کاربر (جلوگیری از SQL injection، XSS) |
| محدودیت نرخ | ۱۰۰ req/min برای هر IP (Cloudflare) |

[Ref: REQ-NFR-003]

---

### ۲.۴ قابلیت اطمینان (Reliability)

| معیار | هدف |
| :-- | :-- |
| زمان فعال بودن | ۹۹.۵٪ (MVP)، ۹۹.۹٪ (فاز ۲) |
| از دست دادن داده (RPO) | ≤۱۵ دقیقه (پشتیبان‌گیری DB هر ۱۵ دقیقه) |
| زمان بازیابی (RTO) | ≤۱ ساعت (failover خودکار) |
| نرخ خطا | <۰.۵٪ (شکست‌های شبکه استثنا) |

[Ref: REQ-NFR-004]

---

### ۲.۵ قابلیت نگهداری (Maintainability)

| الزام | پیاده‌سازی |
| :-- | :-- |
| مستندات کد | کامنت‌های درون‌خطی («چرا»، نه «چه»)؛ README در ریشه repo |
| مستندات API | مشخصات OpenAPI خودکار (Swagger) در `/api/docs` |
| لاگ‌گذاری | لاگ‌های JSON ساختاریافته (Winston/Bunyan)، ارسال به Azure Log Analytics |
| نظارت | داشبوردهای Prometheus + Grafana (تأخیر، نرخ خطا، عمق صف) |
| هشدار | یکپارچه‌سازی PagerDuty برای شکست‌های حیاتی (DB down، API 5xx >۱٪) |

[Ref: REQ-NFR-005]

---

### ۲.۶ قابلیت استفاده (Usability - UX)

| الزام | پیاده‌سازی |
| :-- | :-- |
| پاسخگویی موبایل | همه اجزای UI برای موبایل بهینه‌شده (۷۰٪ کاربران روی گوشی هوشمند) |
| دسترسی‌پذیری | تطابق WCAG 2.1 سطح AA (کنتراست رنگ، ناوبری صفحه‌کلید، پشتیبانی screen reader) |
| زمان بارگذاری | First Contentful Paint (FCP) ≤۱.۵s روی اتصال ۴G |
| دیده‌شدن نشان اعتماد | نشان باید در viewport بدون scroll دیده شود (تفکر سیستم ۱) |
| وضوح توضیح | مودال تفکیک اعتماد از زبان ساده استفاده می‌کند، نه اصطلاحات فنی |
| پیام‌رسانی خطا | پیام‌های خطای دوستانه کاربر (مثلاً «اوه! مشکلی پیش آمد» به جای کدهای خطا) |

[Ref: REQ-NFR-006]

---

## ۳. آزمون پذیرش (Acceptance Testing)

### ۳.۱ سناریوهای End-to-End

**سناریو ۱: دریافت → امتیازدهی → خوشه‌بندی → نمایش**

1. URL مقاله RSS جدید به منبع تست اضافه کن
2. تأیید کن آیتم در جدول `items` ظرف ۱۵ دقیقه ظاهر می‌شود
3. تأیید کن رکورد `trust_signals` با `final_score` در محدوده ۱۵-۹۵ ایجاد شد
4. تأیید کن `breakdown_json` شامل همه ۴ جزء است (پایه، منشأ، تأیید متقابل، شفافیت)
5. تأیید کن آیتم به `narrative_id` موجود یا جدید اختصاص داده شد
6. تأیید کن روایت در پاسخ endpoint `/feed` ظاهر می‌شود
7. تأیید کن نشان اعتماد رنگ صحیح را در UI Framer نمایش می‌دهد

**معیار قبولی:** همه مراحل بدون خطا در <۲ دقیقه کامل شوند

---

**سناریو ۲: تشخیص پروکسی**

1. منبع پروکسی رژیم شناخته‌شده اضافه کن (مثلاً شبکه reprint تسنیم)
2. کار دسته‌ای تشخیص پروکسی هفتگی را اجرا کن
3. تأیید کن `ProxyScore ≥۷۰` محا
<span style="display:none">[^3][^4][^5][^6]</span>

<div align="center">⁂</div>

[^1]: 02-requirements.md

[^2]: srs-nura-v2.4.md

[^3]: 01-discovery-and-planning.md

[^4]: 03-architecture-overview.md

[^5]: 04-ai-engineering.md

[^6]: Iran Propaganda Archive Jan2026

